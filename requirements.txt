# Python Dependencies — MOSIP PE Video Repurposing Pipeline
# Install with: pip install -r requirements.txt

# Core API Clients
deepgram-sdk>=3.2.0        # Deepgram STT (Nova-2) + TTS (Aura) — video_editing.py
groq>=0.9.0                # Groq LLM inference (Llama-3.3-70b) — video_editing.py, process_video.py
google-generativeai>=0.7.0 # Gemini 2.0 Flash chapter extraction — video_editing.py

# Environment & Config
python-dotenv>=1.0.0       # .env file loading — all Python files

# Standard Library (no install needed — included with Python 3.8+)
# os, subprocess, re, sys, json, argparse, time, requests
